[project]
name = "helpdesk-edu"
version = "0.2.0"
description = "A HelpDesk EDU application"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
]

[dependency-groups]
dev = ["pytest>=8.3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
